# Translation models configuration template
# Copy this file to your project root as 'translator.models.yaml' and adjust as needed

# Models are tried in the order listed (first = cheapest/most preferred).
models:
  default:
    - openai:gpt-4o-mini
    - claude:claude-3-5-haiku-latest
    - openai:o3-mini
    - openai:gpt-4o
    - claude:claude-3-5-sonnet-latest

  # Example language override
  chinese:
    - qwen/qwen-2.5-7b-instruct
    - openai:gpt-4o-mini
